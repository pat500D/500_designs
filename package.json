{
  "devDependencies": {
    "browserstack-cypress-cli": "^1.25.3",
    "cypress": "^13.3.0",
    "cypress-email-results": "^1.8.0"
  },
  "dependencies": {
    "cypress-plugin-xhr-toggle": "^1.0.0"
  },
  "scripts": {
    "cy:run": "cypress run",
    "browserstack": "npx browserstack-cypress run"
  }
}
